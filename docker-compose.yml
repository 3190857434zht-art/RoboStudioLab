name: robostudio

services:
  backend:
    build: ./backend
    container_name: robostudio_backend
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/app
      - /var/run/docker.sock:/var/run/docker.sock
    environment:
      # The start scripts set this to an absolute host path so algorithm
      # containers can mount the same exchange directory.
      - HOST_EXCHANGE_DIR=${HOST_EXCHANGE_DIR}
    command: uvicorn main:app --host 0.0.0.0 --port 8000 --reload

  frontend:
    build: ./frontend
    container_name: robostudio_frontend
    ports:
      - "8501:8501"
    volumes:
      - ./frontend:/app
    environment:
      # For local deployment, the browser accesses the backend status endpoint via localhost to auto-refresh job results.
      - BACKEND_PUBLIC_URL=http://localhost:8000
    depends_on:
      - backend